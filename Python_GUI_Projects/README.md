# Five Python GUI Projects

Open any numbered folder and run its `main.py` with Python 3. Every application uses only the standard library, displays the required student header, validates input, and includes its own detailed README.
